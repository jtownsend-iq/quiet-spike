<?xml version="1.0" encoding="utf-8"?>
<!--
  android/quiet-spike/app/src/main/AndroidManifest.xml
  Single-Activity, single-screen capture spike. No share intent yet
  (per CLAUDE.md "What this session does NOT do") — that is its own
  micro-spike per SPIKE-01 § "What the spike deliberately does not prove".
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="Quiet Spike"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuietSpike">

        <activity
            android:name=".CaptureActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|locale|layoutDirection|fontScale|uiMode|density|smallestScreenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
